当模型原生不支持 tool_calls 这种标准化工具调用能力时，通过自定义工具描述 + 封装功能 + 标准化调用协议（如 MCP/Skill 等），是更成熟、更落地的替代方案。下面我会把这个思路拆解清楚，结合实际场景说明为什么这是更优解，以及具体该怎么做。
一、核心逻辑：绕过原生 tool_calls，用「自然语言 + 结构化输出 + 工具封装」实现能力扩展
模型不支持 tool_calls 本质是无法直接输出标准化的工具调用指令（如 {"name":"get_weather","parameters":{"city":"北京"}}），但我们可以通过「引导模型输出结构化内容 + 本地解析执行」的方式，实现和 tool_calls 等效的能力，核心流程如下：
用户提问

向模型传入「工具清单」（name+description+参数说明）

引导模型输出结构化调用指令（如JSON）

本地代码解析指令，校验参数合法性

调用封装好的工具函数/服务（如MCP/Skill）

工具返回结果

模型基于结果生成最终回答








二、为什么「自定义封装工具 + MCP/Skill」是更成熟的方案？
兼容性强：不管模型是否原生支持 tool_calls，都能适配（比如 MiniMax abab5、一些开源轻量模型等），无需依赖模型的特定能力；
可控性高：工具的调用权限、参数校验、异常处理都由你自己的代码掌控，避免模型原生 tool_calls 可能出现的「误调用」「参数错误」等问题；
扩展性好：MCP（Model Context Protocol）、Skill 这类方案本质是标准化的「模型 - 工具」交互协议，能统一管理多个工具，甚至对接外部服务（如 API、数据库、本地脚本），比零散的 @tool 注释更易维护；
落地性强：企业级场景中，很少直接用模型原生的 tool_calls，而是会封装成内部的 Skill/MCP 服务，统一收口工具调用逻辑（比如权限校验、调用日志、限流等）。
三、具体落地步骤（以 Python 为例，通用可复用）
1. 第一步：封装工具函数（核心能力）
先把需要扩展的能力封装成独立函数，做好参数校验和异常处理：
python
运行
# 示例：封装「天气查询」「计算器」两个工具
import requests

# 工具1：天气查询
def get_weather(city: str) -> str:
    """
    根据城市名称查询实时天气
    :param city: 城市名称（如北京、上海），必填
    :return: 天气信息字符串
    """
    if not city:
        return "错误：城市名称不能为空"
    # 模拟调用天气API（实际替换为真实接口）
    try:
        # 示例接口，仅做演示
        resp = requests.get(f"https://api.example.com/weather?city={city}", timeout=5)
        return f"{city}当前天气：{resp.json()['weather']}，温度：{resp.json()['temp']}℃"
    except Exception as e:
        return f"天气查询失败：{str(e)}"

# 工具2：计算器
def calculator(expression: str) -> str:
    """
    执行简单数学计算（仅支持加减乘除）
    :param expression: 计算表达式（如1+2*3），必填
    :return: 计算结果
    """
    if not expression:
        return "错误：计算表达式不能为空"
    try:
        # 仅演示，实际需做更严格的表达式校验，避免安全风险
        result = eval(expression)
        return f"计算结果：{expression} = {result}"
    except Exception as e:
        return f"计算失败：{str(e)}"

# 工具注册表：统一管理所有工具（方便查找和调用）
TOOL_REGISTRY = {
    "get_weather": get_weather,
    "calculator": calculator
}

# 工具描述（传给模型，让模型理解每个工具的用途和参数）
TOOL_DESCRIPTION = """
你可以调用以下工具来完成任务，调用格式为JSON字符串（必须严格符合JSON格式）：
{
    "tool_name": "工具名称",
    "parameters": {"参数名": "参数值"}
}

可用工具列表：
1. get_weather：查询城市天气
   - 参数：city（字符串，城市名称，如北京）
2. calculator：执行数学计算
   - 参数：expression（字符串，计算表达式，如1+2*3）

如果不需要调用工具，直接返回自然语言回答即可。
"""
2. 第二步：引导模型输出结构化指令
向模型提问时，把「工具描述」和「用户问题」一起传入，强制模型输出标准化的 JSON（如果模型输出不标准，可加多次提示）：
python
运行
import json
import requests

# 模拟调用MiniMax API（替换为真实接口）
def call_mini_max(prompt: str) -> str:
    API_KEY = "你的API Key"
    GROUP_ID = "你的Group ID"
    url = f"https://api.minimax.chat/v1/text/chatcompletion?GroupId={GROUP_ID}"
    payload = {
        "model": "abab5",  # 假设是不支持tool_calls的旧模型
        "messages": [{"role": "user", "content": prompt}],
        "stream": False
    }
    headers = {
        "Content-Type": "application/json",
        "Authorization": f"Bearer {API_KEY}"
    }
    resp = requests.post(url, headers=headers, json=payload)
    return resp.json()["choices"][0]["message"]["content"]

# 组装提示词：用户问题 + 工具描述
user_question = "计算100*(20+30)，并查询深圳的天气"
prompt = f"""
用户问题：{user_question}
{TOOL_DESCRIPTION}
请严格按照指定JSON格式返回工具调用指令，不要添加其他无关内容。
"""

# 调用模型获取工具调用指令
model_response = call_mini_max(prompt)
print("模型输出：", model_response)
3. 第三步：解析指令并调用工具
解析模型返回的 JSON 指令，匹配工具注册表中的函数执行，最后把结果传回模型生成最终回答：
python
运行
# 解析模型输出的工具调用指令
try:
    tool_call = json.loads(model_response)
    tool_name = tool_call["tool_name"]
    parameters = tool_call["parameters"]
    
    # 调用对应的工具
    if tool_name in TOOL_REGISTRY:
        tool_result = TOOL_REGISTRY[tool_name](**parameters)
        print("工具调用结果：", tool_result)
        
        # 把工具结果传回模型，生成最终回答
        final_prompt = f"""
        用户问题：{user_question}
        工具调用结果：{tool_result}
        请基于工具结果，用自然语言回答用户问题。
        """
        final_answer = call_mini_max(final_prompt)
        print("最终回答：", final_answer)
    else:
        print(f"未知工具：{tool_name}")
except json.JSONDecodeError:
    # 模型未输出标准JSON，直接用模型的自然语言回答
    print("模型未调用工具，直接回答：", model_response)
except Exception as e:
    print("工具调用异常：", str(e))